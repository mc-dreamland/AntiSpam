name: AntiSpam
version: ${project.version}
main: com.mengcraft.antispam.AntiSpam
api-version: 1.13

permissions:
  spam.bypass:
    default: false
  spam.admin:
    description: Operator permission.
    default: op

commands:
  spam:
    permission: spam.admin
    description: Operator command for this plug-in.

softdepend: [SimpleORM]
